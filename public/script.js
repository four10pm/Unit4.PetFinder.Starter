const petinfo = document.querySelector("petinfo")

const petform = document.querySelector("petform")
petform.addEventListener("submit", onPetSubmit )
const onPetSubmit = (value) => {

}


const ownerform = document.querySelector("ownerform")